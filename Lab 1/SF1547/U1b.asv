Startgissning = 1;
counter = 1;
clear x;
x(1) = Startgissning;
disp(i);
while counter < 100
    clear answer;
    answer = (1/9) .* (power(x(counter),2) - 3.*sin(3.* x(counter) + 2) - 1) + x(counter);
    x(counter + 1) = answer;
    counter = counter + 1;
end

answer

